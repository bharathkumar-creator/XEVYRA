{
  "name": "@xevyra/domain",
  "version": "1.0.0",
  "description": "Pure Domain Layer for XEVYRA platform (Zero external runtime dependencies)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
